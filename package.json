{
  "name": "sohda",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "engines": {
    "node": ">=12"
  },
  "dependencies": {
    "ts-jest": "^26.5.1"
  }
}
