import WebConnector from './web';
import AppConnector from './app';

/**
 * Called from the web sided script. This function will
 * tell Sohda that an event tunnel needs to be opened
 * between the current web page and the app.
 */
export async function createConnector() {}

export function connectorFor(name: string) {}
